package com.email.app.notification.observability;

import org.springframework.stereotype.Component;

@Component
public class NotificationMetrics {
    // TODO expose counters/timers: processed, retries, dlq, smtp latency, smtp failures, consumer lag.
}

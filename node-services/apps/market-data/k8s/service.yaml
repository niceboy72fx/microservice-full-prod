apiVersion: v1
kind: Service
metadata:
  name: node-market-data
  namespace: __NAMESPACE__
  labels:
    app: node-market-data
spec:
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: node-market-data
  type: ClusterIP

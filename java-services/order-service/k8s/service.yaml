apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: __NAMESPACE__
  labels:
    app: order-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: order-service
  type: ClusterIP

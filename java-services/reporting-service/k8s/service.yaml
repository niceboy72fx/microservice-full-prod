apiVersion: v1
kind: Service
metadata:
  name: reporting-service
  namespace: __NAMESPACE__
spec:
  selector:
    app: reporting-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

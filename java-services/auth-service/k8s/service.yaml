apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: __NAMESPACE__
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

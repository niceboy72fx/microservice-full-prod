apiVersion: v1
kind: Service
metadata:
  name: node-notification
  namespace: __NAMESPACE__
  labels:
    app: node-notification
spec:
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: node-notification
  type: ClusterIP

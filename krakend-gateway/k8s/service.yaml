apiVersion: v1
kind: Service
metadata:
  name: krakend-gateway
  namespace: __NAMESPACE__
  labels:
    app: krakend-gateway
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: krakend-gateway
  type: LoadBalancer

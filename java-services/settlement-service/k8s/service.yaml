apiVersion: v1
kind: Service
metadata:
  name: settlement-service
  namespace: __NAMESPACE__
spec:
  selector:
    app: settlement-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-project-template
  namespace: __NAMESPACE__
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-project-template
  template:
    metadata:
      labels:
        app: node-project-template
    spec:
      containers:
        - name: node-project-template
          image: __IMAGE__
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
              name: node-project-template-config
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20

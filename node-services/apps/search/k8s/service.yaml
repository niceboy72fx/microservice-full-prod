apiVersion: v1
kind: Service
metadata:
  name: node-search
  namespace: __NAMESPACE__
  labels:
    app: node-search
spec:
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: node-search
  type: ClusterIP

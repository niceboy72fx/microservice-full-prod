apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: __NAMESPACE__
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

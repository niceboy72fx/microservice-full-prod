apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: __NAMESPACE__
spec:
  selector:
    app: account-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
